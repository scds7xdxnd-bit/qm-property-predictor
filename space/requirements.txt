rdkit>=2024.3.1
numpy>=1.26
pandas>=2.1
scikit-learn>=1.4
# The app now uses XGBoost rather than a random forest: better on the
# scaffold split, and it exposes exact TreeSHAP so the attribution table
# is arithmetic rather than a story.
xgboost>=2.0
